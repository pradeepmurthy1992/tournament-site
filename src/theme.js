// Shared color palette — sportier/higher-energy than the original navy
// "GamePort" theme. Kept in one place so every component (auth forms,
// nav shell, tournament views) draws from the same swatches.
export const ACCENT = "#ff5a1f"; // vivid orange — primary brand/interactive color
export const ACCENT_SECONDARY = "#00c9a7"; // teal — secondary highlight (live/explore)
